var integer acc;

integer odd(integer n) {
  var integer j;
  j := 3;
  print(47);
  return(j*n+1);
}

integer even(integer n) {
  print(92);
  return(n/2);
}

main {
  acc := arg;
  while (acc != 1) {
    if (acc%2 == 0) {
      acc := even(acc);
    } else {
      acc := odd(acc);
    }
  }
}

