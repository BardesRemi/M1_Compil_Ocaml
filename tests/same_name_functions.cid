var integer acc;

integer odd(integer n, integer a) {
  var integer j;
  j := 3;
  print(47);
  return(j*n+1);
}

integer odd(integer n) {
  print(92);
  return(n/2);
}

main {
  acc := arg;
  while (acc != 1) {
    if (acc%2 == 0) {
      acc := odd(acc);
    } else {
      acc := odd(acc, 1);
    }
  }
}

